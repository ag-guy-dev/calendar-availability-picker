Office.onReady(() => {
  // No custom actions needed — the ribbon button's Action type is
  // "ShowTaskpane", handled natively by Office. This file still must
  // exist and load Office.js per the add-in platform's requirements.
});
